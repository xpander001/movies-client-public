import React from 'react';

// props a desarrollar
// value,
// name,
// id,
// label - la accessibilidad es importante! Por todo lo que sabéis, pero además lo necesitaremos
// para los teses. Asociemos el form con un label!
// labelHidden si es true, el label tiene que tener la clase sr-only,
// customClasses por defecto el input tiene clase 'form-control'. Añade clases opcionales
// (Sin quitar la original!)
const Input = () => {
  return <div></div>;
};

export default Input;
