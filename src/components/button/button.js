import React from 'react';

// Hagamos un boton que reciba las siguientes props:
// type, default a 'button'
// primary aplica la clase btn-primary
// outline aplica la clase btn-outline-primary
// fullWidth aplica la clase w-100
// onClick es una funcion con default funcion vacia
// children los children de react
// disabled boolead que disabilita el boton
// size: si 'large', aplica 'btn-lg'. si 'small', aplica 'btn'
const Button = () => {
  return <div>Un boton</div>;
};

export default Button;
